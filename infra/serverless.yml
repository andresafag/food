org: andresafag
app: my-app
service: express-pug-app

frameworkVersion: '4'
custom:
  serverless-offline:
    noPrependStageInUrl: true

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1 
  stage: dev
  environment:
    API_KEY: ${env:API_KEY}
    AWS_LAMBDA_EXEC_WRAPPER: /opt/otel-instrument
    OTEL_EXPORTER_OTLP_ENDPOINT: "http://44.210.111.254:9090"
    OTEL_EXPORTER_OTLP_PROTOCOL: "http/protobuf"

  apiGateway:
    binaryMediaTypes:
      - '*/*'

functions:
  api:
    handler: handler.handler
    events:
      - http:
          path: /
          method: any
      - http:
          path: '{proxy+}'
          method: any
    layers:
      - arn:aws:lambda:us-east-1:615299751070:layer:AWSOpenTelemetryDistroJs:15    

package:
  individually: true
  patterns:
    - '!**'                
    - '../app.js'
    - '../handler.js'
    - '../package.json'
    - '../node_modules/**'
    - '../views/**'        
    - '../public/**'       

